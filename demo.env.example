# Optional: disable bundled demo dataset in development (use real Supabase only)
# NEXT_PUBLIC_USE_DUMMY_DATA=false

# Force demo dataset in production builds (normally off)
# NEXT_PUBLIC_USE_DUMMY_DATA=true
